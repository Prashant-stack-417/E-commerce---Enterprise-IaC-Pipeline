apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: default
  labels:
    app: ecommerce-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: ecommerce-app
